# Core dependencies for molecular optimization agent system
# RDKit - try one of these options:
# Prefer rdkit-pypi on PyPI to avoid conflicts; comment out the other if used via conda
rdkit-pypi
# If rdkit-pypi fails for your environment, switch to:
# rdkit
# Option 3: Install via conda: conda install -c conda-forge rdkit

pandas
numpy
scipy
scikit-learn
matplotlib
xgboost

# LangChain and LangGraph dependencies
langchain
langchain-core
langchain-openai
langchain-community
langgraph
langchain-experimental
langchain_nvidia_ai_endpoints
langchain_huggingface

# OpenAI integration
openai

# Vector database and embeddings
chromadb
sentence-transformers

# ArXiv document loading
arxiv

# PDF processing (for ArXiv documents)
PyMuPDF

# Environment variables
python-dotenv

# Statistical analysis
statsmodels

# Data processing
openpyxl
xlrd

# Optional: For better performance
numba
joblib

# Development and testing (optional)
pytest
black
flake8 

# Quantum chemistry backends (optional but supported in code)
# psi4
# If you want to use the PySCF route for Gibbs (optional)
# pyscf

# xTB via tblite and ASE for feasibility single-points
tblite
ase

# Streamlit GUI
streamlit

# Web utils
requests
tqdm